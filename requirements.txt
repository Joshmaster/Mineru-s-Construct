# ─────────────────────────────────────────────────────────────────────────────
#  Mineru's Construct — dependências Python
#  Instalar: pip3 install -r requirements.txt
#  Ou deixe o setup.sh instalar automaticamente.
# ─────────────────────────────────────────────────────────────────────────────

# Discord
discord.py>=2.3.2

# HTTP / async
aiohttp>=3.9.0
requests>=2.31.0
httpx>=0.27.0

# Web server (API local do bot)
flask>=3.0.0

# WhatsApp (bridge HTTP client)
httpx>=0.27.0

# Webhook server
aiohttp>=3.9.0

# QR code (usado em outros lugares)
qrcode>=7.4.2
segno>=1.6.1

# Imagens dos lembretes
Pillow>=10.0.0
